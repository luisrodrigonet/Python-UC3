# Organização e Manutenção de Templates com Django Partials

## :one: Introdução: O Problema da Manutenção do Head

Imagine que você está desenvolvendo um site com várias páginas diferentes: página inicial, página de produtos, página de contato, página de perfil do usuário, entre outras. Em todas essas páginas, você precisa incluir elementos comuns no `<head>` do HTML, como:

🔶 Meta tags (charset, viewport, description)
🔶 Título da página
🔶 Links para arquivos CSS
🔶 Links para frameworks como Bootstrap
🔶 Scripts JavaScript
🔶 Favicons

Agora, vamos supor que você decidiu copiar e colar todo esse código em cada template. Parece uma solução rápida, certo? Mas e se você precisar fazer uma alteração simples, como adicionar um novo arquivo CSS ou atualizar a versão do Bootstrap?

⚠️ **O problema**: 
- Você teria que abrir **cada um dos templates** e fazer a mesma modificação manualmente. 
- Em um projeto com 20, 30 ou até 100 páginas diferentes, isso se torna um verdadeiro pesadelo de manutenção!


📙 **Exemplo de Página**: `produto.html`
```html
<!-- produto.html -->
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Produtos</title>
    <link rel="stylesheet" href="styles.css">
    <link rel="stylesheet" href="bootstrap.min.css">
</head>
<body>
    <h1>Produtos</h1>
    <!-- Conteúdo específico da página -->
</body>
</html>
```

📙 **Exemplo de Página**: `contato.html`
```html
<!-- contato.html -->
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contato</title>
    <link rel="stylesheet" href="styles.css">
    <link rel="stylesheet" href="bootstrap.min.css">
</head>
<body>
    <h1>Contato</h1>
    <!-- Conteúdo específico da página -->
</body>
</html>
```

Se você precisar alterar o caminho do CSS ou adicionar uma nova meta tag, terá que editar **todos os arquivos**! Isso viola o princípio `DRY` (**Don't Repeat Yourself** - Não Se Repita) e torna o código extremamente difícil de manter.

## :two: A analogia com CSS: Por Que Usamos Arquivos Externos?

Vamos fazer uma comparação que vai deixar tudo mais claro. Você já deve ter aprendido sobre CSS e sabe que existem três formas de aplicar estilos em HTML:

### 🗂️ CSS Inline (Pior Opção)
```html
<p style="color: blue; font-size: 16px;">Texto com estilo inline</p>
<p style="color: blue; font-size: 16px;">Outro texto com mesmo estilo</p>
<p style="color: blue; font-size: 16px;">Mais um texto com mesmo estilo</p>
```

**Problemas:**
☢️ Código repetitivo
☢️ Difícil manutenção
☢️ Se quiser mudar a cor de azul para vermelho, precisa alterar em todos os lugares

### 🗂️ CSS Interno (Opção Intermediária)
```html
<head>
    <style>
        .texto-destaque {
            color: blue;
            font-size: 16px;
        }
    </style>
</head>
<body>
    <p class="texto-destaque">Texto com estilo</p>
    <p class="texto-destaque">Outro texto</p>
</body>
```

**Melhor que inline**, mas ainda tem problemas:
🔥 CSS precisa ser carregado novamente em cada página
🔥 Não aproveita o cache do navegador

### 🗂️ CSS Externo (Melhor Opção) ✅

Exemplo de arquivo de CSS: `styles.css`
```html
<!-- arquivo: styles.css -->
.texto-destaque {
    color: blue;
    font-size: 16px;
}
```

Exemplo de pagina web utilizando CSS externo:
```html
<!-- Todas as páginas HTML -->
<head>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <p class="texto-destaque">Texto com estilo</p>
</body>
```

**Vantagens:**
🚀 **Reutilização**: Um único arquivo CSS serve para múltiplas páginas
🚀 **Manutenção centralizada**: Mudanças em um único lugar afetam todo o site
🚀 **Performance**: Navegador faz cache do arquivo CSS
🚀 **Organização**: Separação clara entre estrutura (HTML) e apresentação (CSS)

## :three: Templates Django

O `Django` aplica exatamente o mesmo conceito de reutilização e manutenção centralizada para templates HTML através de três recursos poderosos:

1. **Template Inheritance (Herança de Templates)**: Similar aos arquivos `CSS` externos
2. **Include Tag**: Para componentes reutilizáveis
3. **Partials (Template Fragments)**: Para fragmentos específicos e contextualizados

### 3.1. Template Inheritance: Criando a Base

A herança de templates funciona como uma hierarquia familiar. Você cria um template "pai" (base) que contém a estrutura comum, e os templates "filhos" herdam essa estrutura, apenas preenchendo as partes específicas.

#### 💻 Estrutura do Projeto

```
meu_projeto/
│
├── templates/
│   ├── base.html               # Template base (pai)
│   ├── partials/               # Pasta para componentes reutilizáveis
│   │   ├── _header.html
│   │   ├── _footer.html
│   │   └── _navbar.html
├── produtos/
│   └── templates/
│       └── produtos/
│           ├── lista.html      # Template filho
│           └── detalhe.html    # Template filho
```

#### 💾 Criando o Template Base


**Arquivo**: templates/base.html 🏗️

```html
<!-- templates/base.html -->
{% load static %}
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{% block meta_description %}Site de Produtos{% endblock %}">
    
    <title>{% block title %}Minha Loja{% endblock %}</title>
    
    <!-- Bootstrap 5.3 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" 
          rel="stylesheet">
    
    <!-- CSS Customizado -->    
    <link rel="stylesheet" href="{% static 'css/styles.css' %}">
    
    <!-- Bloco para CSS adicional específico de cada página -->
    {% block extra_css %}{% endblock %}
    <!-- Final do Bloco para CSS -->


</head>
<body>
    <!-- Header -->
    <header>
        <!-- Partial - Navbar -->
        {% include 'partials/_navbar.html' %}
        <!-- Final do Partial - Navbar -->
    </header>

    <!-- Conteúdo Principal -->
    <main class="container my-4">
        <!-- Bloco Content -->
        {% block content %}
            <!-- Conteúdo específico de cada página vai aqui -->
        {% endblock %}
        <!-- Final do Bloco Content -->
    </main>

    <!-- Footer -->
    <footer class="bg-dark text-white mt-5">
        <!-- Partial - Footbar -->
        {% include 'partials/_footer.html' %}
        <!-- Partial - Footbar -->
    </footer>

    <!-- Bootstrap 5.3 JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Bloco JavaScript adicional específico de cada página -->
    {% block extra_js %}{% endblock %}
    <!-- Final do Bloco JavaScript -->
</body>
</html>
```

**Elementos-chave do template base:**

- **`{% block nome_do_bloco %}`**: Define áreas que os templates filhos podem sobrescrever
- **`{% include %}`**: Inclui componentes reutilizáveis
- **`{% load static %}`**: Carrega o sistema de arquivos estáticos do Django
- **`{% static 'caminho' %}`**: Gera URLs corretas para arquivos estáticos

#### 💾 Criando Templates Filhos

Agora vamos criar páginas específicas que herdam de `base.html`:

```html
<!-- templates/produtos/lista.html -->

<!--  Template Base-->
{% extends 'base.html' %}

<!-- Titulo da Página -->
{% block title %}Lista de Produtos - Minha Loja{% endblock %}

<!-- Descrição da Página -->
{% block meta_description %}Confira nossa seleção completa de produtos{% endblock %}

<!-- CSS específico da página -->
{% block extra_css %}
<style>
    .produto-card {
        transition: transform 0.2s;
    }
    .produto-card:hover {
        transform: scale(1.05);
    }
</style>
{% endblock %}

<!-- Conteúdo da Página -->
{% block content %}
<div class="row">
    <div class="col-12">
        <h1 class="mb-4">Nossos Produtos</h1>
    </div>
    
    {% for produto in produtos %}
    <div class="col-md-4 mb-4">
        <div class="card produto-card">
            <img src="{{ produto.imagem.url }}" class="card-img-top" alt="{{ produto.nome }}">
            <div class="card-body">
                <h5 class="card-title">{{ produto.nome }}</h5>
                <p class="card-text">{{ produto.descricao|truncatewords:20 }}</p>
                <p class="text-primary fw-bold">R$ {{ produto.preco }}</p>
                <a href="{% url 'produto_detalhe' produto.id %}" 
                   class="btn btn-primary">Ver Detalhes</a>
            </div>
        </div>
    </div>
    {% empty %}
    <div class="col-12">
        <p class="alert alert-info">Nenhum produto disponível no momento.</p>
    </div>
    {% endfor %}
</div>
{% endblock %}

<!-- Java Script da página -->
{% block extra_js %}
<script>
    console.log('Página de produtos carregada!');
    // JavaScript específico para esta página
</script>
{% endblock %}
```

**Como funciona:**

1. `{% extends 'base.html' %}` **deve ser a primeira linha** - indica que este template herda de `base.html`
2. Cada `{% block %}` sobrescreve o bloco correspondente do template pai
3. Blocos não sobrescritos mantêm o conteúdo padrão do template base

#### 💾 Exemplo: Página de Detalhes do Produto

```html
<!-- templates/produtos/detalhe.html -->
{% extends 'base.html' %}

{% block title %}{{ produto.nome }} - Minha Loja{% endblock %}

{% block meta_description %}{{ produto.descricao|truncatewords:30 }}{% endblock %}

{% block content %}
<div class="row">
    <div class="col-md-6">
        <img src="{{ produto.imagem.url }}" 
             class="img-fluid rounded" 
             alt="{{ produto.nome }}">
    </div>
    <div class="col-md-6">
        <h1>{{ produto.nome }}</h1>
        <p class="lead">{{ produto.descricao }}</p>
        <h3 class="text-primary">R$ {{ produto.preco }}</h3>
        
        <div class="mt-4">
            <form method="post" action="{% url 'adicionar_carrinho' %}">
                {% csrf_token %}
                <input type="hidden" name="produto_id" value="{{ produto.id }}">
                <div class="mb-3">
                    <label for="quantidade" class="form-label">Quantidade:</label>
                    <input type="number" 
                           class="form-control" 
                           id="quantidade" 
                           name="quantidade" 
                           value="1" 
                           min="1">
                </div>
                <button type="submit" class="btn btn-success btn-lg">
                    Adicionar ao Carrinho
                </button>
            </form>
        </div>
        
        <div class="mt-4">
            <h4>Especificações:</h4>
            <ul>
                <li><strong>Categoria:</strong> {{ produto.categoria }}</li>
                <li><strong>Estoque:</strong> {{ produto.estoque }} unidades</li>
                <li><strong>SKU:</strong> {{ produto.sku }}</li>
            </ul>
        </div>
    </div>
</div>

<div class="row mt-5">
    <div class="col-12">
        <h3>Produtos Relacionados</h3>
        {% include 'partials/_produtos_relacionados.html' with produtos=relacionados %}
    </div>
</div>
{% endblock %}
```

### 3.2. Include Tag: Componentes Reutilizáveis

O `{% include %}` permite inserir templates menores dentro de templates maiores, criando componentes modulares e reutilizáveis.

#### 🛸 Criando um `Navbar` Reutilizável


**Arquivo**: `templates/partials/_navbar.html`

```html
<!-- templates/partials/_navbar.html -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{% url 'home' %}">

            <img src="{% static 'images/logo.png' %}" 
                alt="Logo" height="30">

            Minha Loja
        </a>
        
        <button class="navbar-toggler" 
                type="button" 
                data-bs-toggle="collapse" 
                data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link" 
                        href="{% url 'home' %}">Início</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" 
                        href="{% url 'produtos_lista' %}">Produtos</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" 
                        href="{% url 'contato' %}">Contato</a>
                </li>

                <!-- 
                    Esta parte do NavBar muda de acordo 
                    se o usuário está autenticado ou não 
                -->
                {% if user.is_authenticated %}
                <li class="nav-item">
                    <a class="nav-link" href="{% url 'perfil' %}">
                        Olá, {{ user.first_name }}!
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{% url 'logout' %}">Sair</a>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{% url 'login' %}">Entrar</a>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
```

#### 🛸 Criando um Footer Reutilizável

**Arquivo**: `templates/partials/_footer.html`

```html
<!-- templates/partials/_footer.html -->
<div class="container py-4">
    <div class="row">
        <div class="col-md-4">
            <h5>Sobre Nós</h5>
            <p>Somos uma loja comprometida em oferecer os melhores produtos com qualidade e preço justo.</p>
        </div>
        <div class="col-md-4">
            <h5>Links Rápidos</h5>
            <ul class="list-unstyled">
                <li>
                    <a href="{% url 'sobre' %}" 
                        class="text-white-50">Sobre</a>
                </li>
                <li>
                    <a href="{% url 'termos' %}" 
                        class="text-white-50">Termos de Uso</a>
                </li>
                <li>
                    <a href="{% url 'privacidade' %}" 
                        class="text-white-50">Política de Privacidade</a>
                </li>
            </ul>
        </div>
        <div class="col-md-4">
            <h5>Contato</h5>
            <p class="text-white-50">
                Email: contato@minhaloja.com<br>
                Telefone: (99) 1234-5678
            </p>
        </div>
    </div>
    <hr class="bg-white">
    <div class="text-center">
        <p>&copy; 2025 Minha Loja. Todos os direitos reservados.</p>
    </div>
</div>
```

#### 🛸 Include com Variáveis

Você pode **passar variáveis** específicas para templates incluídos usando a palavra-chave `with`:


**Arquivo de Template**: `templates/partials/_card_produto.html`
```html
<!-- templates/partials/_card_produto.html -->
<div class="card h-100">

    <img src="{{ produto.imagem.url }}" 
            class="card-img-top" 
            alt="{{ produto.nome }}">

    <div class="card-body">
        <h5 class="card-title">{{ titulo }}</h5>
        <p class="card-text">{{ descricao }}</p>
        <p class="text-primary fw-bold">{{ preco }}</p>

        {% if mostrar_botao %}
        <a href="{{ link }}" class="btn btn-primary">{{ texto_botao }}</a>
        {% endif %}
        
    </div>
</div>
```

**Uso do partial com variáveis:** `templates/produtos/lista.html`

```html
<!-- templates/produtos/lista.html -->
{% for produto in produtos %}
<div class="col-md-4 mb-4">
    {% include 'partials/_card_produto.html' with 
        titulo=produto.nome     descricao=produto.descricao 
        preco=produto.preco     mostrar_botao=True 
        link=produto.get_absolute_url 
        texto_botao="Ver Mais" 
    %}
</div>
{% endfor %}
```

### 3.3. Django Template Partials: Fragmentos Nomeados

**Novidade do Django 6.0!** 
Template Partials permitem definir fragmentos reutilizáveis **dentro do mesmo arquivo**, melhorando a organização e a manutenibilidade.

#### 🧵 Instalação (para Django < 6.0)

```bash
pip install django-template-partials
```

```python
# settings.py
INSTALLED_APPS = [
    'template_partials',
    # ...
]
```

#### 🧵 Definindo e Usando Partials

```html
<!-- templates/produtos/lista.html -->
{% extends 'base.html' %}
{% load partials %}

{# Definindo um partial para o card de produto #}
{% partialdef produto-card %}
<div class="card h-100 shadow-sm">
    <img src="{{ produto.imagem.url }}" 
         class="card-img-top" 
         alt="{{ produto.nome }}"
         style="height: 200px; object-fit: cover;">
    <div class="card-body d-flex flex-column">
        <h5 class="card-title">{{ produto.nome }}</h5>
        <p class="card-text flex-grow-1">{{ produto.descricao|truncatewords:15 }}</p>
        <div class="mt-auto">
            <p class="text-primary fw-bold mb-2">R$ {{ produto.preco }}</p>
            <div class="d-grid gap-2">
                <a href="{% url 'produto_detalhe' produto.id %}" 
                   class="btn btn-primary btn-sm">
                    Ver Detalhes
                </a>
                <button class="btn btn-outline-success btn-sm" 
                        onclick="adicionarCarrinho({{ produto.id }})">
                    Adicionar ao Carrinho
                </button>
            </div>
        </div>
    </div>
</div>
{% endpartialdef produto-card %}

{% block content %}
<div class="row">
    <div class="col-12 mb-4">
        <h1>Catálogo de Produtos</h1>
        <p class="lead">Encontre os melhores produtos com os melhores preços</p>
    </div>
    
    {# Usando o partial definido acima #}
    {% for produto in produtos %}
    <div class="col-md-4 col-lg-3 mb-4">
        {% partial produto-card %}
    </div>
    {% empty %}
    <div class="col-12">
        <div class="alert alert-warning">
            <h4>Nenhum produto encontrado</h4>
            <p>Volte mais tarde para conferir nossas novidades!</p>
        </div>
    </div>
    {% endfor %}
</div>

{# Seção de produtos em destaque usando o mesmo partial #}
<div class="row mt-5">
    <div class="col-12 mb-3">
        <h2>Produtos em Destaque</h2>
    </div>
    {% for produto in produtos_destaque %}
    <div class="col-md-6 col-lg-4 mb-4">
        {% partial produto-card %}
    </div>
    {% endfor %}
</div>
{% endblock %}
```

#### 🎯 Vantagens dos Partials

⚡ **Localidade de Comportamento**: O código relacionado fica próximo, facilitando leitura e manutenção
⚡ **DRY (Don't Repeat Yourself)**: Evita duplicação de código
⚡ **Reutilização**: Mesmo partial pode ser usado várias vezes no mesmo template
⚡ **Modularidade**: Fragmentos independentes e testáveis
⚡ **Integração com HTMX**: Perfeito para atualizações parciais de página

#### 🧩 Partial com Renderização Direta

Você também pode renderizar um partial diretamente de uma view:

```python
# views.py
from django.shortcuts import render
from .models import Produto

def adicionar_produto_ajax(request):
    if request.method == 'POST':
        nome = request.POST.get('nome')
        preco = request.POST.get('preco')
        descricao = request.POST.get('descricao')
        
        produto = Produto.objects.create(
            nome=nome,
            preco=preco,
            descricao=descricao
        )
        
        # Renderiza apenas o partial, não o template inteiro
        return render(request, 'produtos/lista.html#produto-card', 
                     {'produto': produto})
```

**Sintaxe**: `template.html#nome-do-partial`

